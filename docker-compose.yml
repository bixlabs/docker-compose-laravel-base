version: '3'

networks:
  laravel:

services:
  site:
    build:
      context: ./docker
      dockerfile: nginx.dockerfile
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    container_name: new_project-nginx
    ports:
      - 8081:80
    volumes:
      - ./src:/var/www/html:delegated
    depends_on:
      - php
      - db
      # uncomment if you want the redis service
      # - redis
      # uncomment if you want the mailhog service
      # - mailhog
    networks:
      - laravel

  db:
    image: mysql:latest
    container_name: new_project-db
    restart: unless-stopped
    tty: true
    ports:
      - 3307:3306
    volumes:
      - ~/.docker/data/new_project/db:/var/lib/mysql
    environment:
      MYSQL_DATABASE: new_project-local-db
      MYSQL_USER: new_project-local-user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
      SERVICE_TAGS: local
      SERVICE_NAME: db
    networks:
      - laravel

  php:
    build:
      context: ./docker
      dockerfile: php.dockerfile
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    container_name: new_project-php
    volumes:
      - ./src:/var/www/html:delegated
    networks:
      - laravel

  composer:
    build:
      context: ./docker
      dockerfile: composer.dockerfile
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    container_name: new_project-composer
    volumes:
      - ./src:/var/www/html
    working_dir: /var/www/html
    depends_on:
      - php
    user: laravel
    entrypoint: ['composer', '--ignore-platform-reqs']
    networks:
      - laravel

  artisan:
    build:
      context: ./docker
      dockerfile: php.dockerfile
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    container_name: new_project-artisan
    volumes:
      - ./src:/var/www/html:delegated
    depends_on:
      - db
    working_dir: /var/www/html
    entrypoint: ['php', '/var/www/html/artisan']
    networks:
      - laravel

  npm:
    image: node:16.13.1
    container_name: new_project-npm
    volumes:
      - ./src:/var/www/html
    ports:
      - 3001:3000
      - 3002:3001
    working_dir: /var/www/html
    entrypoint: ['npm']
    # user: "node"
    networks:
      - laravel

  redis:
    image: redis:alpine
    container_name: new_project-redis
    restart: unless-stopped
    ports:
      - 6380:6379
    networks:
      - laravel

  mailhog:
    image: mailhog/mailhog:latest
    container_name: new_project-mailhog
    ports:
      - 1026:1025
      - 8026:8025
    networks:
      - laravel
